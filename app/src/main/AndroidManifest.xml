<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Feature 1: Background Photo permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Re-arm the daily refresh alarm after device reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Ask the user to exempt the app from battery optimization so the wallpaper
         service survives Samsung Freecess / Xiaomi MIUI kill-on-background. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- POST_NOTIFICATIONS (Android 13+): without this the user can't even toggle
         notifications on in Samsung settings, which leaves the foreground-service
         notification (below) silently suppressed. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- A foreground service holding a min-priority notification is the only
         supported way to opt OUT of Samsung Freecess + Android doze restrictions
         on the wallpaper engine's process. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- In-app updater: poll GitHub Releases API on launch + download new APKs. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LifeDots"
        tools:targetApi="31">

        <!-- Main Activity with Onboarding -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LifeDots">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/settings_title"
            android:theme="@style/Theme.LifeDots" />

        <!-- Live Wallpaper Service -->
        <service
            android:name=".wallpaper.LifeDotsWallpaperService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/wallpaper" />
        </service>

        <!-- Lets us hand a downloaded APK to the system installer via a content://
             URI. Required for ACTION_INSTALL_PACKAGE from Android 7+. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Foreground keep-alive service — pins the wallpaper engine's process
             in foreground state so Samsung Freecess / MIUI MemoryGuard / stock
             Android Doze don't freeze it. -->
        <service
            android:name=".service.KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="keep_wallpaper_engine_alive" />
        </service>

        <!-- Date Change Receiver -->
        <receiver
            android:name=".receiver.DateChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.example.lifedots.DAILY_TICK" />
                <action android:name="com.example.lifedots.AUTO_SWITCH_TICK" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
