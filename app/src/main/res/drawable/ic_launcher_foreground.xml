<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive icon foreground: full-bleed logo bitmap.
     The launcher will mask this into circle/squircle shapes; the source bitmap
     already has a generous safe zone around the skull so it survives cropping. -->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/ic_launcher_logo"
    android:gravity="fill" />
