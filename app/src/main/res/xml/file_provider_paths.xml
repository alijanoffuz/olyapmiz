<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- The updater writes the downloaded APK here; FileProvider exposes it as
         a content:// URI that the system installer can read. -->
    <external-files-path name="updates" path="updates/" />
</paths>
